"""JAX Ground Truth Corpus — production-ready recipes for JAX ML workflows.

Modules:
    transforms: Core JAX transforms (jit, grad, vmap, pmap)
    autodiff: Automatic differentiation (jacobians, hessians, custom VJPs)
    arrays: NumPy-compatible array operations on accelerators
    neural: Neural network building blocks (activations, normalization)
    training: Optimizers, training loops, checkpointing
    distributed: Multi-device parallelism (pmap, shard_map, mesh)
    random: Functional PRNG system
"""

__version__ = "0.1.0"
